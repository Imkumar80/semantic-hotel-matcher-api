services:
  - type: web
    name: semantic-hotel-matcher
    env: docker
    plan: free
    region: ohio
    branch: main
    dockerCommand: "uv run uvicorn api.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
